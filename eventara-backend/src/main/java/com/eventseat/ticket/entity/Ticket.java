package com.eventseat.ticket.entity;

// @Entity: id, bookingItem (@OneToOne), qrCodeData (Base64/path),
// qrHash (unique, for verification), ticketStatus (TicketStatus enum),
// scannedAt, scannedByOrganizerId
