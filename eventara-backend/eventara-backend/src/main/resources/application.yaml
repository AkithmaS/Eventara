spring:
  datasource:
    url: jdbc:postgresql://ep-noisy-king-aioxgp34-pooler.c-4.us-east-1.aws.neon.tech/eventara_db?sslmode=require
    username: neondb_owner
    password: npg_ZOHvV7luiQU6
    driver-class-name: org.postgresql.Driver
    hikari:
      maximum-pool-size: 5
      minimum-idle: 1
      idle-timeout: 30000
      max-lifetime: 60000
      connection-timeout: 30000
      keepalive-time: 30000
      connection-test-query: SELECT 1

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    open-in-view: false  
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true

server:
  port: 8080
